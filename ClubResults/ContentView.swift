import SwiftUI

struct ContentView: View {
    var body: some View {
        TabsView()
    }
}

#Preview {
    ContentView()
        .clubGlassBackground() // preview-only so it looks right in canvas
}
